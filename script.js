/* 
  Optional: External Script 
  Currently, the custom cursor interactivity is in index.html to ensure 
  the HTML runs identically out-of-the-box. If the project grows, move 
  the script tag logic here and uncomment the script import in index.html.
*/
